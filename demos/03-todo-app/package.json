{
  "name": "03-todo-app",
  "version": "1.0.0",
  "description": "Electron Learning - Todo App with React + TypeScript",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "npm run build && electron .",
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.renderer.json && npm run copy-assets",
    "copy-assets": "cp src/renderer/*.html dist/renderer/ && mkdir -p dist/renderer/styles && cp src/renderer/styles/*.css dist/renderer/styles/",
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^42.2.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {}
}